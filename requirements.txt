# ── Document Parsing ──────────────────────────────────────────
pymupdf==1.24.5          # Read PDFs
python-docx==1.1.2       # Read DOCX files

# ── Embeddings (runs locally, no API needed) ──────────────────
sentence-transformers==3.0.1   # BGE-Large embedding model + reranker

# ── Vector Store (runs locally, no server needed) ─────────────
qdrant-client==1.10.1    # Stores and searches embedding vectors

# ── Keyword Search ────────────────────────────────────────────
rank-bm25==0.2.2         # BM25 keyword-based search

# ── LLM (runs locally via Ollama) ────────────────────────────
ollama==0.2.1            # Python client for Ollama (phi3:mini)

# ── UI ────────────────────────────────────────────────────────
streamlit==1.28.1        # Web interface

# ── Utilities ─────────────────────────────────────────────────
tqdm==4.66.4             # Progress bars
